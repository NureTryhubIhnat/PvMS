# Використовуємо офіційний легковаговий образ Node.js
FROM node:18-slim

# Робоча директорія всередині контейнера
WORKDIR /app

# Копіюємо файл сервера в контейнер
COPY tcp_server.js .

# Відкриваємо порт 8000 [cite: 985]
EXPOSE 8000

# Запускаємо додаток
CMD ["node", "tcp_server.js"]
